package org.github.chezecakes.blocks;

import net.minecraft.block.Block;

// marble for the kaaba structure
public class MarbleBlock extends Block {
    public MarbleBlock(Block.Settings settings) {
        super(settings);
    }
}
